- 2 Synthesis and vectorized computation
- 2.1 Review: the general idea of **digital audio as an array of numbers**. Remind students of x[n] = x(n/f_s) notation from chapter 1, any other pertinent details
- 2.2 Synthesis, move the conten from 1.4 (Synthesis: making sound from math) to here
  - change loop variable `i` to `n` for consistency w/ notation
- 2.3 Vectorized computation and NumPy
  - Introduce the high-level idea of vectorized computation: executing the same operation on entire arrays of data, instead of individual data elements
  - Mention the benefits: faster (bypasses slow interpreted loops in favor of pre-compiled low-level operation execution) and more readable (many lines of code become one!)
  - Introduce NumPy, show the synthesis example from above in equivalent Numpy code (using `n = np.arange(N)` and `np.sin(...)`)
- 2.4 NumPy intro
  - Creating arrays (both w/ predefined list data in Python or alloc using `np.zeros` or `np.random.randn() * 0.01`)
    - Noise example is a good time to add **Warning: Potential for hearing damage. Never do computer music programming using headphones. You could seriously damage your ears.**
  - Slicing arrays
  - Operations on arrays
  - Multidimensional arrays / slicing / reshaping
    - W/ stereo audio as pedagogical example, left ch as 220 Hz right as 330 Hz
    - Use PyQuist convention of (num_samples, num_channels), see `pyquist/audio.py`
  - Multidimensional array operations / broadcasting
    - W/ stereo synthesis using `np.sin`, two frequencies, and `np.newaxis` as broadcasting example, stereo-to-mono as pedagogical example of `np.mean`
- 2.5 Pyquist intro
  - Intro to Pyquist general high-level ideas, see PyQuist `README.md` and documentation
    - Not a high-level computer music library, instead, lightweight wrapper around NumPy, basic utilities to facilitate low-level computer music programming and audio IO in Python
  - Explain the `Audio` object in more detail, i.e., just an array of audio samples (`Audio.samples`) plus a sample rate (`Audio.sample_rate`), see PyQuist documentation for more details
  - Show basic synthesis example in Python, NumPy, and PyQuist `Audio`
  - Show how mixing works (e.g., add two audio objects together)
  - Show how other things like slicing work (slicing an `Audio` vs `Audio.segment`)
- Lots of code and audio examples here!
